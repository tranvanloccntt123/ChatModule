import React from "react";
import { View } from "react-native";
import { ScreenInterface } from "../common/ScreenInterface";
export default function ChatDetail(props: ScreenInterface){
    React.useEffect(() => {
        
    }, []);
    return <View>
        
    </View>
}